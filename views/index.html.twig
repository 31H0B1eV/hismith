{% extends "layout.html.twig" %}

{% block content %}
    <div class="welcome">
        <h1>Welcome to our new Silex Application!</h1>

        {% if comments %}
        <ul>
            {% for comment in comments %}
                <li>
                    <p>{{ dump(comment) }}</p>
                </li>
            {% endfor %}
        </ul>
        {% endif %}
    </div>
{% endblock %}
